export class red {
  private smt: string;
}

const sddfd = 0

sddfd

const foo = () => {
  return (
    <>
      <NavBar style={style} />
      <div className="navbar">sddfdfdfd</div>
      {Date.now()}
      {[1, 2, 3].map((item, index) => {
        ;`${item} + ${index}`
      })}
    </>
  )
}

const aaa = {
  dfdfdf: kdjdkfjd
}
